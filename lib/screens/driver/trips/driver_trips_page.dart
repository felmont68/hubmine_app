import 'package:flutter/material.dart';

class DriverTripsPage extends StatelessWidget {
  const DriverTripsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Driver Trips page"),
    );
  }
}
