import 'package:flutter/material.dart';

class DriverStatsPage extends StatelessWidget {
  const DriverStatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Driver Stats page"),
    );
  }
}
